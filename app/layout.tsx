import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SYS Vehicles | Premium Used Cars in Sheffield",
  description:
    "Browse our handpicked selection of quality used cars. Finance available. Part exchange welcome. Based in Sheffield — clean, well-maintained cars exactly as described.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
